import React, {
	Components
} from 'react';

import {
	StyleSheet,
	View,
	Text,
	Image,
} from 'react-native';

class CustomRow extends Components {
	constructor(props) {
	  super(props);
	
	  this.state = {};
	}

	render() {
		const {
			section,
			content
		} = this.props;

	}
}